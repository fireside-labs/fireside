"use client";
import ComingSoon from "@/components/ComingSoon";

export default function PipelinePage() {
    return (
        <ComingSoon
            title="Task Builder"
            icon="📋"
            description="Automate multi-step AI tasks with spec → build → test → review pipelines. Backend integration required."
        />
    );
}
